# This workflow has been replaced by seo-autofix.yml
# Kept here only to prevent broken run history
# It does nothing.
name: Auto SEO Optimizer

on:
  workflow_dispatch:  # manual only — no longer runs on push

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "This workflow is deprecated. Use seo-autofix.yml instead."
